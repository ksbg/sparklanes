- Better user-configurable logging
- README
- Example notebook
- Docs
- Test parallel task execution
- Allow arguments for both class initialization, as well as for the entry method
- Allow users to overwrite sparklane internal methods
- Allow users to add extra sparklane config options (e.g. verbosity, unpersisting of already executed tasks, etc.)